@{
    ViewData["Title"] = "Settings";
}

<section class="mb-4 d-flex justify-content-between align-items-center">
    <div>
        <h1 class="h4 mb-1">Global settings</h1>
        <p class="small text-muted mb-0">
            High-level configuration for Recipe Planner (placeholder UI – backend model exists).
        </p>
    </div>
    <a href="/Admin" class="small text-muted">← Back to dashboard</a>
</section>

<section class="soft-card">
    <p class="small text-muted mb-0">
        The <code>Setting</code> model is defined in the backend. In a full version,
        this page would load key/value pairs via an API and allow editing site-wide
        options (e.g., max recipe size, feature flags, password rules).
    </p>
</section>
